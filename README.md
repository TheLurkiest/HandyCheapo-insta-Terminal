# HandyCheapo-insta-Terminal

hit the primary button at any time to skip over lcd messages.  Use ctrl+c to end the program and ctrl+d to exit the terminal."

If you're using a keyboard at startup without a mouse or monitor you will be unable to enter text using the keyboard until you have 
performed the following precise steps to restart this program.  These steps must be performed IN ORDER.  If you're using a raspberry
pi the steps should just be as follows.

...just make sure you create a shell script that will run your python code the second you open up the terminal as I mentioned earlier.

1) hit the Win key on your keyboard using the left side Win key if available... the right side Win key should also work if it is the only one on your keyboard.   
2) hit the down arrow key 5 times.   
3) hit the right arrow key 1 time.   
4) hit the down arrow key 7 times.   
5) hit enter."

Choose from options using Morse Code: "

mainMenu Options: Option 0- Full access to terminal using keyboard   Option 1- Write to file   Option 2- read from file   Option 3- Perform online search 
Option 4- check email   Option 5- send email   Option 6- execute terminal commands   Option 7- play music   Option 8- quit   Option 9- change settings

